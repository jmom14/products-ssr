
export enum fakestoreapi {
    PRODUCTS = "https://fakestoreapi.com/products",
    CATEGORIES = "https://fakestoreapi.com/products/categories",
}

export enum api {
    PRODUCTS = "/api/products",
    CATEGORIES = "/api/categories",
}